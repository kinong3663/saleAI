三个问题一起答，但第一个问题之前有一道红线必须先说。

## 先澄清：能不能找人分工

如果「一个弄前端另一个弄后端」指的是**另一个人**——**不行，这是废卷级的问题**。题目第二十节写得很明确：

> **不允许**：邀请其他开发者与你并行：写代码、**分模块开发**、Debug、部署、共同设计整个方案

「分模块开发」被逐字列出。唯一允许的真人互动是「就单个知识点向他人请教」，而且请教了要在 `AI_CODING_NOTES.md` 里说明。

如果指的是**两个 AI 会话并行**——允许，题目第十七节明确写了「使用 AI」不扣分。但**这个项目不值得这么拆**，理由我在第 2 问里说。

---

## 问题 1：怎么让 AI 用这三个文档

现在有个实际问题：**文档在 `D:\zigoai\docs\`，但那里还没有 git 仓库，AI 每次开新会话也不会自动读它们。**

要让文档真正起作用，需要一个**入口文件**——在仓库根目录放 `AGENTS.md`（Codex / Cursor 都认）或 `CLAUDE.md`（Claude Code 认），内容大致是：

```markdown
# ZigoAI Mini

这是一个 24H 实战项目。开工前先读：
- docs/决策记录.md      ← 所有决定的结论，冲突时以它为准
- docs/实施文档.md      ← 施工段与核心文件定稿
- docs/技术栈构建与说明.md
- docs/数据表构建与说明.md

## 铁律
1. 只有 src/server/agent/ 里可以出现 LLM 调用
2. 所有 service 函数第一个参数必须是 tenantId
3. LLM 调用绝不允许放在数据库事务里
4. 只做当前施工段，不要提前实现后面的
5. 文档之间有冲突时，停下来问我，不要自己决定

## 验收
每段完成后逐条跑 docs/实施文档.md 里该段的验收清单。
```

**这个文件的价值不是"给 AI 看"，是让规则每次会话自动生效。** 没有它，你每次都要重新交代一遍上下文，而且总会漏掉一两条。

顺便：**先把 `docs/` 提交进 git**，并且把仓库初始化了。文档进仓库之后，它们就变成 AI 的"外部记忆"——你可以随时开新会话，不必担心上下文丢失。

### 每段开工时的标准话术

```text
读 docs/决策记录.md 和 docs/实施文档.md。
现在执行 S2，只做 S2，不要提前做后面的。
完成后把 S2 的验收清单逐条真跑一遍，把结果贴给我。
文档有冲突就停下来问，不要自己决定。
```

最后一句很关键。AI 遇到文档冲突时，默认行为往往是"自己选一个合理的"——但**你的设计决策是答辩素材，不能被 AI 悄悄改掉**。

---

## 问题 2：分工

**不要拆前端/后端。** 四个具体理由：

**① 前端不是这个项目的大头。** 题目第二十三节明确说「精美 UI / 动画」不是重点。而真正的工作量在 agent pipeline、状态机、护栏、租户规则上。拆一个人专门做前端，等于把一半人力放在评分最低的地方。

**② 接口没冻结就并行，必然返工。** 并行的前提是契约完全冻结。文档里虽然写了 API 形状，但真写起来一定会调——调的时候双方要来回同步，沟通成本超过分工收益。

**③ 单元是"施工段"不是"层"。** 按 S0→S11 串行，每一段都是**端到端的竖切**（比如 S2 同时包含 service + API + 页面）。竖切交接只需要看验收结果，横切交接需要重建半个系统的理解。

**④ 这道题最后要看你能不能讲清楚自己的代码。** 题目第二十九节会围绕真实项目交流。分成两个人写，各自只有半套理解，答辩时必然露怯。

### 那正确的分工是什么

```text
你         做决定、做验收、理解代码
AI         写实现、排障、跑验收清单
```

这个分工里你的工作量其实不小：

```text
· 每段开工前确认要做什么
· 每段结束后跑验收清单（不是读代码，是真跑）
· 让 AI 解释这段的关键决策，对照决策记录的编号
· 决定遇到冲突怎么办
```

**最容易被忽略的一条纪律**：每段做完，不要只确认"测试通过了"，还要让 AI 给你讲一遍这段代码在干什么，你听懂了再进下一段。**这不是学习环节，这是答辩彩排**——面试官问「Customer State 为什么这样设计」的时候，你要能当场指着代码讲。

### 唯一值得并行的例外

S7（异常处理）、S8（Follow-up）、S9（/dev 页）、S10（测试）之间没有依赖，如果你想省时间，可以让 AI 开并行的子 agent 同时做。但这是**优化项不是必需项**——而且并行做完之后，理解成本会上升。**如果时间够，串行做反而更稳。**

---

## 问题 3：实施文档怎么用，哪些阶段合批

### 合批原则

一个批次应该是一个**「能独立验收的完整能力」**。太细会浪费每次的开销，太粗会失控。我建议这么分：

| 批次 | 包含 | 为什么这么合 |
|---|---|---|
| **① ** | S0 + S1 | 脚手架 + 数据层。S0 的目录结构要等 S1 建完才能验证，分两次反而麻烦 |
| **②** | S2 | 单独一段。客户与消息是后面所有功能的地基，量大，且这一步做完就能满足「持久化不丢」的硬要求 |
| **③** | S3 + S4 | Pipeline + 护栏。护栏本来就要接进 `analyze`，一起写更顺。但**必须分两次验收**：先验证能调通 LLM，再验证护栏生效 |
| **④** | S5 | 状态闭环。核心任务 3 和 4 的落点，单独做 |
| **⑤** | S6 | 部署。★ 门禁，单独做，做完必须用手机流量验证 |
| **⑥** | S7 + S9 | 异常处理 + /dev 页。/dev 本来就是为了演示 S7/S8 才存在的 |
| **⑦** | S8 | Follow-up。依赖 S7 的 fallback 路径和 S9 的调试工具 |
| **⑧** | S10 | 测试 |
| **⑨** | S11 | 登录 + README + AI_NOTES |

**两条硬边界**：

```text
· S6 之前不许跳到任何进阶段 —— 上线是硬要求，进阶是软要求
· 批次 ③ 内部虽然合批，但两次验收不能省 —— 
  「能调通」和「规则生效」是两件事，混在一起验收会漏掉护栏
```

### 每段做完的四个动作

```text
① 跑验收清单        逐条真跑，把输出贴出来，不是读代码确认
② 讲一遍            让 AI 解释这段的关键决策，你对照决策记录的编号听懂
③ git commit        一段一个 commit，message 写清段号
④ 决定能否往下走     验收没全过 → 只修这一段，不带着问题进下一段
```

**第 ③ 条有额外价值**：题目要求提交 `Final Commit Hash`。一段一个 commit 之后，如果某段把系统搞坏了，你能干净地回滚；而如果全程只有一个大 commit，出问题就只能硬改。

### 怎么判断一段真的做完了

用 S4 举例，它的验收清单是双租户对照：

```text
同一句话「那你们多少钱？」
  乐蒙     → 可提价格，但引导到店体验（命中 R3）
  机械之家  → 不给报价，索取行驶证（命中 R1 / R2）
```

**如果两个租户回复一样，S4 就是没做完**——不管你写了多少护栏代码。因为这一段的真正产出不是"护栏函数存在"，而是「**租户规则确实影响了 AI**」这个可演示的事实。这条也正是题目核心任务 5 的原文要求。

---

## 建议的下一步

现在就能做两件事：

**一是把仓库建起来**：`git init`，把 `docs/` 提交进去，写 `AGENTS.md`。这一步做完，后面每次开工 AI 都能自动带上正确的上下文。

**二是直接开始批次 ①（S0 + S1）**：起 Next.js 骨架、写 schema、跑 migration、seed 两个租户。这一步的验收很实在——`npx prisma studio` 能看到两个租户，且重跑 seed 不炸。

要我现在就把 `AGENTS.md` 和 `.gitignore`、`.env.example` 建起来，然后开始 S0 吗？