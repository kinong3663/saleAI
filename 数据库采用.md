## 为什么是 PostgreSQL

先说最硬的一条：**这个项目有真正的跨表事务需求。**

一条客户消息进来，一次请求里要写四张表：`Message`、`AgentRun`、`CustomerState`、可能还有 `CustomerFact`。而这些写**必须原子**。如果 AgentRun 记着"判成 HIGH_INTENT"但 CustomerState 还停在 INTERESTED，你就有了一个幽灵判断——审计日志和实际状态对不上，而这是排障时唯一的线索。反过来如果先更新 State 再写 AgentRun 时崩了，你连"这次判定是谁做的"都查不到。

**多行多表事务，是选 Postgres 而不是其他 NoSQL 的根本原因。**

另外四条，都是这个项目真实用到的：

**`timestamptz` 原生时间类型。** 销售系统里到处是时间判断——多久没联系、是否超过跟进窗口、last_contact_at 排序。Postgres 有真正的时间类型、时区语义清晰、可以索引、可以范围扫描。SQLite 只能存字符串，比较和时区全是手工活。

**JSONB 能存也能查。** 租户规则、AgentRun 的原始输出都是半结构化数据，天然适合 JSON。但 Postgres 的 JSONB 还能建 GIN 索引、能写 `config->'rules'` 这样的查询。**同一个库里，强类型列和灵活 JSON 共存**，不用为此引入第二种存储。

**部分索引。** 事实表只需要索引 ACTIVE 的行：

```sql
CREATE INDEX ON customer_facts (tenant_id, customer_id, key) WHERE status = 'ACTIVE';
```

**`SELECT ... FOR UPDATE` 行级锁。** 同一个客户可能同时有两条消息触发判定，两个事务都基于同一个旧状态算出新状态，后写的覆盖先写的（lost update）。行锁是标准解法。

还有一条软理由：后续要做"检索历史成交案例"，pgvector 就在同一个库里，不用再引入向量数据库。

### 一个诚实的补充

选 Postgres 有一半理由其实来自**部署决策**，而不是数据库本身的优劣。

如果我只跑单台 VPS 的 Docker，**SQLite 完全够用**，而且更简单——一个文件、零运维。但我要部署到 Vercel 这类 serverless 平台，文件系统不持久，SQLite 直接出局；同时它的全局写锁在高并发下会变成瓶颈。

**所以准确的说法是：因为我选了 serverless 部署，所以需要一台真正的数据库服务器，而在托管可得性、免费额度、能力全面性上，Postgres 是最优解。**

至于 MySQL：能用，差距在 JSONB 的查询与索引能力、部分索引、时间类型语义，以及后续做漏斗分析时窗口函数/CTE 更顺手。MongoDB：这里的核心数据是强关系型的（Tenant → Customer → Message/State/Fact），核心诉求是隔离与一致性，文档模型不占优——JSON 只是少数表的需要。

---

## 数据表设计

### 关系形状

```text
Tenant ─┬─< Customer ─┬─< Message
        │             ├── CustomerState        (1:1)
        │             ├─< AgentRun             (每次判定一条)
        │             ├─< CustomerFact         (客户档案)
        │             └─< ConversationSummary  (中期摘要)
        ├─< Message
        ├─< CustomerState
        ├─< AgentRun
        └─< CustomerFact
```

**Customer 是聚合根。** 所有业务数据挂在它下面，而它挂在 Tenant 下面。这条链就是租户隔离的物理形状。

注意：`Message`、`AgentRun` 这些表上**冗余了 `tenantId`**，虽然逻辑上可以从 Customer 推出来。冗余的目的是让每一次查询都能直接带租户条件，不需要 join 才能确定归属。

### 完整 Schema

```prisma
enum MessageRole {
  CUSTOMER
  SALES
  SYSTEM
}

// ─────────────── 租户 ───────────────
model Tenant {
  id        String   @id @default(cuid())
  slug      String   @unique          // swimming / machinery
  name      String
  industry  String?
  salesGoal String                    // 自然语言销售目标
  tone      String?
  // rules[] / stageDefs{} / needHumanTriggers[] / factKeys[] / products[] / maxFollowUps
  config    Json     @db.JsonB
  createdAt DateTime @default(now()) @db.Timestamptz(3)
  updatedAt DateTime @updatedAt      @db.Timestamptz(3)

  customers Customer[]
  messages  Message[]
  states    CustomerState[]
  runs      AgentRun[]
  facts     CustomerFact[]
  summaries ConversationSummary[]

  @@map("tenants")
}

// ─────────────── 客户 ───────────────
model Customer {
  id         String    @id @default(cuid())
  tenantId   String
  name       String
  channel    String    @default("manual")   // 预留 wechat / phone
  externalId String?                        // 渠道侧 ID
  tags       String    @default("")         // 逗号分隔，避免数组类型差异
  archivedAt DateTime? @db.Timestamptz(3)   // 软删除
  createdAt  DateTime  @default(now()) @db.Timestamptz(3)
  updatedAt  DateTime  @updatedAt     @db.Timestamptz(3)

  tenant   Tenant                @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  messages Message[]
  state    CustomerState?
  runs     AgentRun[]
  facts    CustomerFact[]
  summaries ConversationSummary[]

  @@unique([tenantId, externalId])            // 渠道去重
  @@index([tenantId, archivedAt, updatedAt])  // 客户列表页
  @@map("customers")
}

// ─────────────── 消息 ───────────────
model Message {
  id          String      @id @default(cuid())
  tenantId    String                            // 冗余：隔离 + 免 join
  customerId  String
  role        MessageRole
  content     String
  turnId      String?                           // 连续消息聚合
  source      String      @default("manual")    // manual | ai_suggested | followup
  clientMsgId String?                           // 幂等：前端重发不产生重复
  createdAt   DateTime    @default(now()) @db.Timestamptz(3)

  tenant   Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customer Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@unique([customerId, clientMsgId])
  @@index([tenantId, customerId, createdAt])    // 会话页拉最近 N 条
  @@index([customerId, turnId])
  @@map("messages")
}

// ─────────────── 客户状态（1:1） ───────────────
model CustomerState {
  customerId String @id                          // 主键即外键，强制 1:1
  tenantId   String

  leadStage       String  @default("NEW")
  intent          String?
  needHuman       Boolean @default(false)
  humanReason     String?
  humanResolvedAt DateTime? @db.Timestamptz(3)   // 销售手动解除人工

  followUpCount  Int       @default(0)
  lastFollowUpAt DateTime? @db.Timestamptz(3)

  lastCustomerMessageAt DateTime? @db.Timestamptz(3)
  lastSalesMessageAt    DateTime? @db.Timestamptz(3)
  lastActivityAt        DateTime? @db.Timestamptz(3)  // 派生：max(上两者)，为跟进扫描
  lastAnalysisAt        DateTime? @db.Timestamptz(3)

  version   Int      @default(0)                 // 乐观锁 + 变更计数
  updatedAt DateTime @updatedAt @db.Timestamptz(3)

  tenant   Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customer Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@index([tenantId, leadStage, needHuman])
  @@index([tenantId, lastActivityAt])            // 跟进任务扫描
  @@map("customer_states")
}

// ─────────────── AI 判定审计 ───────────────
model AgentRun {
  id               String  @id @default(cuid())
  tenantId         String
  customerId       String
  triggerMessageId String?

  status   String   // SUCCESS | REPAIRED | FALLBACK | FAILED
  model    String
  latencyMs Int
  attempt  Int      @default(1)

  inputDigest     Json    @db.JsonB   // 发给模型的上下文快照
  rawOutput       String?             // 模型原始返回，排障用
  output          Json?   @db.JsonB   // 校验后的结构化结果
  rulesHit        String  @default("")
  guardrailIssues String  @default("")
  error           String?

  // 采纳情况 → 形成反馈闭环
  suggestionSent   Boolean @default(false)
  suggestionEdited Boolean @default(false)

  createdAt DateTime @default(now()) @db.Timestamptz(3)

  tenant   Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customer Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@unique([customerId, triggerMessageId])   // 幂等：一条消息只判定一次
  @@index([tenantId, customerId, createdAt])
  @@index([tenantId, status, createdAt])     // 观察 fallback 率
  @@map("agent_runs")
}

// ─────────────── 客户档案（长期事实） ───────────────
model CustomerFact {
  id         String @id @default(cuid())
  tenantId   String
  customerId String

  category   String   // IDENTITY | NEED | CONSTRAINT | OBJECTION
  key        String   // 受控词表：child_age / budget / concern
  value      String
  evidence   String   // 客户原话，入库前校验是真实消息子串
  sourceMessageId String?
  confidence Float    @default(0.8)
  status     String   @default("ACTIVE")  // ACTIVE | SUPERSEDED | REJECTED
  validFrom  DateTime @default(now()) @db.Timestamptz(3)
  expiresAt  DateTime? @db.Timestamptz(3)

  tenant   Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customer Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@index([tenantId, customerId, key, status])
  @@map("customer_facts")
}

// ─────────────── 中期摘要 ───────────────
model ConversationSummary {
  id         String   @id @default(cuid())
  tenantId   String
  customerId String
  fromAt     DateTime @db.Timestamptz(3)
  toAt       DateTime @db.Timestamptz(3)
  content    String
  msgCount   Int
  createdAt  DateTime @default(now()) @db.Timestamptz(3)

  tenant   Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customer Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@index([tenantId, customerId, toAt])
  @@map("conversation_summaries")
}
```

### 索引是给查询用的，不是装饰

这是设计表时最容易被问的地方。**每一个索引都要能指回一条真实查询**：

| 查询 | 用到的索引 |
|---|---|
| 客户列表：`tenantId` + 按更新时间倒序 | `customers(tenantId, archivedAt, updatedAt)` |
| 会话页拉最近 20 条消息 | `messages(tenantId, customerId, createdAt)` |
| 跟进任务：某租户下非终态、非人工、超时未动的客户 | `customer_states(tenantId, lastActivityAt)` |
| 幂等检查：这条消息判定过没有 | `agent_runs(customerId, triggerMessageId)` 唯一约束 |
| 观察 AI 失败率 | `agent_runs(tenantId, status, createdAt)` |
| 拼装档案：这个客户 ACTIVE 的事实 | `customer_facts(tenantId, customerId, key, status)` |

`lastActivityAt` 是个**刻意的反规范化**——它等于 `max(lastSalesMessageAt, lastCustomerMessageAt)`，本来可以算出来。但存成真列之后，跟进扫描从一个"带 OR 的复杂条件"变成一个干净的索引范围扫描。**代价是多维护一个字段，收益是热路径上的查询变简单。** 这类取舍在答辩时讲出来，比背索引名有价值得多。

### 几个会被追问的细节

**主键为什么用 cuid 不用自增整数？** 自增 ID 会泄漏业务量（竞对能通过 ID 增长估你的单量），跨环境迁移会冲突，而且 ID 出现在 URL 里时，自增让"改个数字试试别人客户"变得毫无成本。cuid 不能替代授权检查，但白送的攻击面没必要留着。

**为什么 `role` 用 enum，而 `leadStage` / `intent` 用 String？** `role` 是固定的三种，数据库级校验最可靠。而阶段和意图是**租户可配置的**——乐蒙和机械之家的阶段语义不同，未来可能新增。用 enum 意味着每加一个值都要 migration，删值还删不掉。所以这类字段用 String + 应用层 Zod 校验。

**时间统一 `timestamptz`，存 UTC，展示时转本地时区。** 别存本地时间字符串，跨时区一定出问题。

**为什么 `CustomerState` 的主键就是 `customerId`？** 用主键即外键的方式，在**数据库层**强制了 1:1 关系。应用层忘写检查也不会出现两个状态行。

### 哪些不变量交给数据库，哪些交给应用

这个划分能说明你是否理解分层的边界：

| 保证方 | 保证什么 |
|---|---|
| **数据库** | 外键存在、非空、唯一性（幂等键）、级联删除、1:1 关系、时间类型 |
| **应用层** | 租户授权（这个用户能碰这个 customerId 吗）、状态迁移合法性（单调性、终态锁）、AI 输出 schema 校验、事实证据校验、业务规则护栏 |

**判断标准是：数据库能廉价保证的、且永远不该被违反的，就下沉到数据库。** 而依赖上下文（租户身份、AI 输出、业务规则）的，只能在应用层做。

最后一条实践建议：24H 里不要手写 SQL 建表，用 Prisma schema + `migrate dev` 生成。schema 文件本身就是你的数据设计文档，答辩时可以直接对着它讲——**而且它和代码里的类型是同一份来源，不会漂移。**